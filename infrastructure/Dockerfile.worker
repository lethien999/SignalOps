# Dockerfile for Worker Service
FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json ./
COPY apps/worker-service ./apps/worker-service
COPY libs ./libs

RUN npm ci --only=production

RUN npm run build -- --project worker-service

CMD ["node", "dist/apps/worker-service/src/main.js"]
